### ゼミナールⅣ 1回目
#### f-string
Pythonでは，文字の中に変数の値を入れて表示したい場面がよくあります．
たとえば，「田中さんは20歳です」と表示したい場合，
以下のプログラムを書くことがあります．

```python
name = "田中"
age = 20
print(name + 'さんは' + str(age)+ '歳です')
```
この2つの変数を使って文章を表示するときに便利なのが 
**f-string（エフストリング）**とよばれる方法です．

- stringは文字列という意味で使用されます．

```python
name = "田中"
age = 20
print(f'{name}さんは{age}歳です')
```

実行すると，次のように表示されます．

```text
田中さんは20歳です
```

ポイントは，文字列の最初に **`f`** を付けることです．

```python
f'文章'
```
または，
```python
f"文章"
```

そして，文章の中に入れたい変数を **`{ }`（波かっこ）** で囲みます．

```python
f'{変数名}'
```

たとえば，
```python
food = 'カレー'
print(f'今日のお昼は{food}です')
```
と書くと，

```text
今日のお昼はカレーです
```

と表示されます．

### 計算もできます

`{ }` の中には，変数だけでなく簡単な計算を書くこともできます．

```python
price = 150
count = 3
print(f'合計金額は{price * count}円です')
```

実行結果は，

```text
合計金額は450円です
```

となります．

#### なぜf-stringを使うのか
f-stringを使うと，文章と変数を自然に組み合わせることができます．

たとえば，

```python
name = '佐藤'
score = 85
print(f'{name}さんの点数は{score}点です')
```

のように書けます．

Pythonでは，結果を画面に表示したり，
文章を作ったりするときによく使うので，
まずは
> **文字列の中に変数の値を入れるための書き方**

と覚えておけば大丈夫です．


#### f-stringのサンプル
##### 例1: 名前を表示する
```python
name = '田中'
print(f'こんにちは，{name}さん．')
```
実行結果：
```text
こんにちは，田中さん．
```
`{name}` の部分に，変数 `name` の中身が入ります．

---

##### 例2: 名前と年齢を表示する

```python
name = '佐藤'
age = 20
print(f'{name}さんは{age}歳です．')
```
実行結果：
```text
佐藤さんは20歳です．
```
複数の変数を1つの文章の中に入れることもできます．

---
##### 例3: 商品の値段を表示する
```python
item = 'りんご'
price = 150
print(f'{item}の値段は{price}円です．')
```
実行結果：
```text
りんごの値段は150円です．
```

文字と数値を組み合わせて表示する場合にも便利です．

---
##### 例4: 計算結果を表示する
```python
price = 120
count = 3
print(f'{price}円の商品を{count}個買いました．')
print(f'合計金額は{price * count}円です．')
```

実行結果：
```text
120円の商品を3個買いました．
合計金額は360円です．
```

`{ }` の中には変数だけでなく，計算式を書くこともできます．

---

##### 例5：小数点以下の桁数を指定する（応用事例）

```python
score = 83.4567
print(f'平均点は{score:.2f}点です．')
```

実行結果：
```text
平均点は83.46点です．
```

`:.2f` と書くと，小数点以下を2桁まで表示できます．


##### 小数点以下の表示桁数

f-stringでは，`:.2f` のように書くことで，小数点以下の表示桁数を指定できます．

| 書き方 | 意味 | 例 | 表示結果 |
|---|---|---|---|
| `{value:.0f}` | 小数点以下0桁 | `f'{3.14159:.0f}'` | `3` |
| `{value:.1f}` | 小数点以下1桁 | `f'{3.14159:.1f}'` | `3.1` |
| `{value:.2f}` | 小数点以下2桁 | `f'{3.14159:.2f}'` | `3.14` |
| `{value:.3f}` | 小数点以下3桁 | `f'{3.14159:.3f}'` | `3.142` |
| `{value:.4f}` | 小数点以下4桁 | `f'{3.14159:.4f}'` | `3.1416` |

##### サンプルプログラム

```python
value = 3.14159
print(f'{value:.0f}')
print(f'{value:.1f}')
print(f'{value:.2f}')
print(f'{value:.3f}')
print(f'{value:.4f}')
````

実行結果：

```text
3
3.1
3.14
3.142
3.1416
```

#### f-string まとめ
f-stringの基本的な書き方は次のとおりです．
```python
name = '田中'
print(f'こんにちは，{name}さん．')
```
ポイントは次の2つです．
- 文字列の前に `f` を付ける
- 文章の中に入れたい変数を `{ }` で囲む

まずは，

```python
f'{変数名}'
```
という形を覚えておきましょう．

#### Pythonのファイル入出力
Pythonでは，プログラムの中に直接文字や数値を書くこともできますが，
実際にはファイルに保存されているデータを使うことも多くあります．

例えば，
- テキストファイルに保存された文章を読み込む
- アンケート結果を読み込む
- 分析した結果をファイルに保存する
- 文章データを読み込んで解析する
といった使い方があります．

Pythonでファイルを読み込んだり，書き込んだりすることを
**ファイル入出力** といいます．

---

##### ファイルを読み込んでみよう

例えば，次のような `file1.txt` があるとします．
```text
こんにちは．
Pythonを勉強しています．
今日はファイル入出力を学びます．
```

このファイルをPythonで読み込むには，次のように書きます．

```python
with open('file1.txt', 'r', encoding='utf-8') as file:
    text = file.read()

print(text)
```

実行結果：
```text
こんにちは．
Pythonを勉強しています．
今日はファイル入出力を学びます．
```

---

##### with open() の基本
ファイルを扱うときには，次の形を基本として覚えておきましょう．

```python
with open('ファイル名', 'モード', encoding='utf-8') as ファイル変数:
    処理
```

例えば，
```python
with open('file1.txt', 'r', encoding='utf-8') as file:
    text = file.read()
```
と書きます．

それぞれには次のような意味があります．

| 書き方 | 意味 |
|---|---|
| `with` | ファイルを安全に扱うための書き方 |
| `open()` | ファイルを開く |
| `'file1.txt'` | 開くファイルの名前 |
| `'r'` | 読み込みモード |
| `encoding='utf-8'` | 文字コードをUTF-8として扱う |
| `as file` | 開いたファイルを `file` という名前で扱う |

---

##### なぜ with を使うのか
ファイルは，開いたあとに閉じる必要があります．
しかし，`with` を使うと，
処理が終わったときにPythonが自動的にファイルを閉じてくれます．
そのため，ファイルを扱うときは，
```python
with open(...)
```
という形を基本として使います．

最初は，
**「ファイルを使うときは `with open()`」**
と覚えておけば大丈夫です．

---

##### encoding='utf-8' とは
コンピュータの中では，
文字はそのままの形で保存されているわけではありません．
コンピュータは，文字を数値に対応させて保存しています．

そのため，

**「どの数値を，どの文字として扱うか」**
というルールが必要になります．
このルールを **文字コード** といいます．

---

##### UTF-8とは
`UTF-8` は，現在広く使われている文字コードの一つです．

英語だけでなく，
- 日本語
- 中国語
- 韓国語
- その他さまざまな言語
の文字を扱うことができます．

Pythonでは，
```python
encoding='utf-8'
```
と書くことで，

**「このファイルをUTF-8という文字コードとして扱ってください」**

と指定しています．

例えば，

```python
with open("sample.txt", "r", encoding="utf-8") as file:
    text = file.read()
```

では，`sample.txt` をUTF-8として読み込んでいます．

---

##### 文字コードが合わないとどうなる？
ファイルを保存したときの文字コードと，
読み込むときの文字コードが異なると，
文字が正しく表示されないことがあります．

例えば，

```text
こんにちは
```

と書かれているはずなのに，

```text
縺薙ｓ縺ｫ縺｡縺ｯ
```

のように表示されることがあります．
このような状態を **文字化け** といいます．

最初のうちは，日本語を含むテキストファイルを扱う場合には，

```python
encoding='utf-8'
```

を指定しておく，と覚えておけば大丈夫です．

---

#### ファイルを読み込む
ファイルを読み込む場合は，`'r'` を使います．
`r` は **read（読む）** の頭文字です．

```python
with open('file1.txt', 'r', encoding='utf-8') as file:
    text = file.read()

print(text)
```

`file.read()` を使うと，ファイル全体を読み込むことができます．

読み込んだ内容は，

```python
text
```

という変数に入っています．

つまり，

```python
text = file.read()
```

は，

**「ファイルの内容を読み込んで，textに入れる」**

という意味です．

---

##### 読み込んだ文章は文字列として使える
ファイルから読み込んだ文章は，
普通の文字列と同じように扱うことができます．

例えば，文字数を調べることができます．

```python
with open('file1.txt', 'r', encoding='utf-8') as file:
    text = file.read()

print(f'文字数は{len(text)}文字です．')
```

`len()` を使うと，文字列の長さを調べることができます．

---

##### 特定の言葉が何回登場するか調べる

例えば，「Python」という言葉が何回登場するか調べてみましょう．

```python
with open('file1.txt', 'r', encoding='utf-8') as file:
    text = file.read()

count = text.count('Python')

print(f'Pythonは{count}回登場します')
```

`count()` を使うと，指定した文字列が何回登場するか数えることができます．

このように，

**ファイルを読み込む → 文章を変数に入れる → Pythonで分析する**

という流れは，文章解析で行う最初の一歩です．

---

##### 1行ずつ読み込む

ファイル全体を一度に読み込むのではなく，1行ずつ読み込むこともできます．

```python
with open('file1.txt', 'r', encoding='utf-8') as file:
    for line in file:
        print(line)
```

この場合，ファイルの内容を上から順番に1行ずつ取り出します．

1行分の文字列が，

```python
line
```

という変数に入ります．

---

##### strip()を使う

1行ずつ読み込む場合，もともとの改行と 
`print()` の改行が重なり，行間が広く見えることがあります・
そのような場合には `strip()` を使います．

```python
with open('file1.txt', 'r', encoding='utf-8') as file:
    for line in file:
        print(line.strip())
```

`strip()` は，文字列の前後にある改行や余分な空白を取り除くために使います．

---

##### ファイルに書き込む

Pythonから新しいファイルを作ることもできます．

ファイルに書き込む場合は `'w'` を使います．

`w` は **write（書く）** の頭文字です．

```python
with open('output.txt', 'w', encoding='utf-8') as file:
    file.write('Pythonからファイルに書き込みました')
```

このプログラムを実行すると，

```text
output.txt
```

というファイルが作成されます．

中身は次のようになります．

```text
Pythonからファイルに書き込みました．
```

---

##### 注意：wは上書きする

`'w'` を使うと，すでに同じ名前のファイルが存在する場合，
その内容は上書きされます．

例えば，`output.txt` の中身が，

```text
こんにちは．
```

だった場合，

次のプログラムを実行します．

```python
with open('output.txt', 'w', encoding='utf-8') as file:
    file.write("こんばんは．")
```

すると，ファイルの中身は，

```text
こんばんは．
```

になります．

もともとの，

```text
こんにちは．
```

は上書きで，元のファイルの中身がすべて消えるので，注意してください．

---

##### ファイルに追加する

もともとの内容を残したまま，新しい文章を追加したい場合には `'a'` を使います．

`a` は **append（追加する）** の頭文字です．

```python
with open('output.txt', 'a', encoding='utf-8') as file:
    file.write("\n新しい文章を追加しました．")
```

例えば，もともと，

```text
Pythonを勉強しています．
```

という内容だった場合，

```text
Pythonを勉強しています．
新しい文章を追加しました．
```

となります．

`\n` は改行を表します．

---

##### 読み込み・書き込み・追記

ファイルを扱うときには，主に次の3種類のモードを使います．

| モード | 英語 | 意味 |
|---|---|---|
| `'r'` | read | ファイルを読み込む |
| `'w'` | write | ファイルに書き込む |
| `'a'` | append | ファイルの最後に追加する |

覚え方は，

- 読む → `r`
- 書く → `w`
- 追加する → `a`

です．

---

##### 基本形を覚えよう

- 読み込み

```python
with open('file1.txt', 'r', encoding='utf-8') as file:
    text = file.read()

print(text)
```

- 書き込み
 
```python
with open('output.txt', 'w', encoding='utf-8') as file:
    file.write('こんにちは')
```

- 追記

```python
with open('output.txt', 'a', encoding='utf-8') as file:
    file.write('\nPythonを勉強しています')
```

---

#### ファイル入出力まとめ

Pythonでファイルを扱うときには，

```python
with open(...)
```

という形を基本として使います．

例えば，ファイルを読み込む場合は，

```python
with open('file1.txt', 'r', encoding='utf-8') as file:
    text = file.read()
```

です．

この1行には，

- `with` → ファイルを安全に扱う
- `open()` → ファイルを開く
- `'r'` → 読み込み
- `encoding='utf-8'` → 文字コードをUTF-8として扱う

という意味があります．

最初は，

**「ファイルを使うときは `with open()`」**

**「日本語を扱うときは `encoding='utf-8'` を指定する」**

と覚えておけば十分です．

また，ファイルを開くモードには，

| やりたいこと | モード |
|---|---|
| 読み込みたい | `'r'` |
| 書き込みたい | `'w'` |
| 追加したい | `'a'` |

があります．

ファイルから文章を読み込めるようになると，

**文章を読み込む → Pythonで処理する → 結果を保存する**

という流れができるようになります．

この流れは，これから学ぶ自然言語処理でもよく使います．


# Python 練習問題

ここでは，これまで学習した

1. f-string
2. ファイル入出力

について練習します．

まずは自分でプログラムを書いて実行してみましょう．

---

# 1. f-stringの練習問題

## 問題1：名前を表示しよう

次の変数があります．

```python
name = "田中"
```

f-stringを使って，次のように表示してください．

```text
こんにちは，田中さん．
```

---

## 問題2：名前と年齢を表示しよう

次の変数があります．

```python
name = "佐藤"
age = 20
```

f-stringを使って，次のように表示してください．

```text
佐藤さんは20歳です．
```

---

## 問題3：出身地を表示しよう

次の変数があります．

```python
name = "鈴木"
city = "札幌"
```

f-stringを使って，次のように表示してください．

```text
鈴木さんの出身地は札幌です．
```

---

## 問題4：商品の価格を表示しよう

次の変数があります．

```python
item = "りんご"
price = 150
```

f-stringを使って，次のように表示してください．

```text
りんごの値段は150円です．
```

---

## 問題5：2つの数値を表示しよう

次の変数があります．

```python
x = 10
y = 5
```

f-stringを使って，次のように表示してください．

```text
xは10，yは5です．
```

---

## 問題6：計算結果を表示しよう

次の変数があります．

```python
price = 120
count = 4
```

f-stringの `{ }` の中で計算を行い，次のように表示してください．

```text
合計金額は480円です．
```

---

## 問題7：足し算の結果を表示しよう

次の変数があります．

```python
a = 12
b = 8
```

f-stringを使って，次のように表示してください．

```text
12 + 8 = 20
```

---

## 問題8：平均点を表示しよう

次の変数があります．

```python
score1 = 80
score2 = 90
score3 = 75
```

3つの点数の平均を計算し，f-stringを使って表示してください．

表示例：

```text
平均点は81.66666666666667点です．
```

---

## 問題9：小数点以下を2桁にしよう

次の変数があります．

```python
score = 83.4567
```

f-stringの `:.2f` を使って，次のように表示してください．

```text
平均点は83.46点です．
```

---

## 問題10：平均値を小数点以下1桁で表示しよう

次の変数があります．

```python
score1 = 72
score2 = 85
score3 = 91
```

3つの点数の平均値を計算し，小数点以下1桁で表示してください．

表示例：

```text
3人の平均点は82.7点です．
```

---

# 2. ファイル入出力の練習問題

以下では，`with open()` を使ってファイルを操作します．

基本形を確認しておきましょう．

```python
with open("ファイル名", "モード", encoding="utf-8") as file:
    処理
```

---

## 問題11：ファイルを読み込もう

次の内容の `sample.txt` を用意してください．

```text
こんにちは．
Pythonを勉強しています．
```

`with open()` と `read()` を使ってファイル全体を読み込み，画面に表示してください．

---

## 問題12：別のファイルを読み込もう

次の内容の `profile.txt` を用意してください．

```text
名前：田中太郎
出身：北海道
趣味：映画鑑賞
```

`profile.txt` を読み込み，内容をすべて表示してください．

---

## 問題13：読み込んだ文章の文字数を調べよう

`sample.txt` を読み込み，`len()` を使って文字数を表示してください．

表示形式は次のようにします．

```text
文字数は○○文字です．
```

f-stringを使って表示してください．

---

## 問題14：特定の言葉を数えよう

次の内容の `python.txt` を用意してください．

```text
Pythonはプログラミング言語です．
Pythonは初心者にも人気があります．
今日はPythonを勉強します．
```

ファイルを読み込み，`count()` を使って，

```text
Python
```

という言葉が何回登場するか調べてください．

表示形式：

```text
Pythonは3回登場します．
```

---

## 問題15：1行ずつ表示しよう

`sample.txt` を1行ずつ読み込み，`for` 文を使って表示してください．

次の形を使います．

```python
with open("sample.txt", "r", encoding="utf-8") as file:
    for line in file:
        # ここに処理を書く
```

---

## 問題16：strip()を使おう

問題15のプログラムを変更して，`strip()` を使って各行の前後の余分な空白や改行を取り除いてから表示してください．

---

## 問題17：新しいファイルを作ろう

`result.txt` というファイルを作成し，

```text
Pythonを勉強しています．
```

という文章を書き込んでください．

`"w"` モードを使用します．

---

## 問題18：複数の文章を書き込もう

`profile.txt` というファイルを作り，次の内容を書き込んでください．

```text
名前：田中
年齢：20
出身：札幌
```

`\n` を使って改行してください．

---

## 問題19：ファイルに文章を追加しよう

次の内容が保存されている `diary.txt` があるとします．

```text
今日はPythonを勉強しました．
```

`"a"` モードを使って，次の文章を追加してください．

```text
ファイル入出力について学びました．
```

実行後のファイルを次のようにしてください．

```text
今日はPythonを勉強しました．
ファイル入出力について学びました．
```

---

## 問題20：読み込みとf-stringを組み合わせよう

次の内容の `message.txt` を用意してください．

```text
今日はPythonの授業です．
```

ファイルを読み込み，読み込んだ文章の文字数を調べてください．

最後にf-stringを使って，

```text
このファイルには○○文字あります．
```

と表示してください．

次の流れでプログラムを作ります．

1. `with open()` でファイルを開く
2. `read()` で文章を読み込む
3. `len()` で文字数を調べる
4. f-stringで結果を表示する

---

# チャレンジ問題

余裕がある人は，次の問題にも挑戦してみましょう．

`input.txt` に保存されている文章を読み込み，

- 文章全体
- 文字数
- 「Python」という言葉の登場回数

を表示してください．

表示例：

```text
文章：
Pythonは楽しいです．
今日はPythonを勉強します．

文字数：○○文字
Pythonの登場回数：2回
```

これまで学習した，

- `with open()`
- `read()`
- `len()`
- `count()`
- f-string

を組み合わせて作ってみましょう．
